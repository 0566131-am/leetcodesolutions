class Solution {
    public int maxSubArray(int[] nums) {
        int maxi=nums[0];
        int n=nums.length;
        for(int i=0;i<n;i++){
            for(int j=i;j<n;j++){
            }
        }
        return maxi;
                 maxi=Math.max(sum,maxi);
                 sum=sum+nums[j];
        
    }
             int sum=0;
}
