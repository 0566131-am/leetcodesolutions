
            if (currentTank < 0) {
                startingStation = i + 1;
                currentTank = 0;
            }
        }

        return totalGas >= totalCost ? startingStation : -1;
    }
}
