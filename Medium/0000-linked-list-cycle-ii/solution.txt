       ListNode fast=head;
       while(fast!=null && fast.next!=null){
        slow=slow.next;
        fast=fast.next.next;
        if(slow==fast){
            slow=head;
            while(slow!=fast){
       ListNode slow=head;
    public ListNode detectCycle(ListNode head) {
                slow=slow.next;
                fast=fast.next;
            }
            return slow;
        }
       } 
       return null;

        
