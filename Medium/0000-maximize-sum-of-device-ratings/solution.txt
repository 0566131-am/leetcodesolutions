           ans += x[1];
        }
        //find the destination 
        int diff = INT_MAX;
        for(auto x : units){
            if(x[1] - mini < diff){
                diff = x[1] - mini;
                toBeSub = x[1];
            }
        }

        return ans + mini - toBeSub;
    }
};
