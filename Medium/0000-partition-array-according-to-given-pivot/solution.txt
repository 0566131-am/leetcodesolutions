     else if(nums[i]<pivot){
       less.add(nums[i]);
        }else{
      
       p.add(nums[i]);
     }
     Stream.of(less,more,p)
     .flatMap(List::stream)
     .mapToInt(Integer::intValue)
     }
       more.add(nums[i]);
       if(nums[i]>pivot)
     for(int i=0;i<nums[i];i++){
        List <Integer>more=new ArrayList<>();
        List <Integer>p=new ArrayList<>();
     .toArray();
