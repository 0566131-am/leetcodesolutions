        else if (nums[i]>pivot)
        more.add(nums[i]);
        else
        p.add(nums[i]);
       }
        less.add(nums[i]);
        if(nums[i]<pivot)
       return Stream.of(less,p,more)
       .flatMap(List::stream)
       .mapToInt(Integer::intValue)
       .toArray();
    }
}
