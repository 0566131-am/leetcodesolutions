                for (int x = i; x <= j; x++) {
                    s += nums[x];
                }

                if (s == k) {
                    len = Math.max(j - i + 1, len);
                }
            }
        }

                int s = 0;

        for (int i = 0; i < n; i++) {
            for (int j = i; j < n; j++) {

        return len;
    }
}
