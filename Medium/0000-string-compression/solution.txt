            }
            if(count==1){
                chars[idx++]=ch;
            }
            else{
                chars[idx++]=ch;
                i++;
                count++;
            while(i<n&& chars[i]==ch){
            int count=0;
            char ch=chars[i];
        for(int i=0;i<n;i++){
        int idx=0;
        int n=chars.length;
    public int compress(char[] chars) {
class Solution {
