                len++;
            else if (c == '#')
                len *= 2;
            else if (c != '%')
            if (c == '*')
                len = Math.max(len - 1, 0L);

        for (int i = 0; i < n; i++) {
            char c = s.charAt(i);
        long[] lens = new long[n];
        long len = 0;
    public char processStr(String s, long k) {
        int n = s.length();
class Solution {
