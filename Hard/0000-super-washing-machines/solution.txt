            return -1;
        }
        int a = t / n; 
        int r = 0;     
        int c = 0;     
        
        for (int w : m) {
            int d = w - a; 
            c += d;
            r = Math.max(r, Math.max(Math.abs(c), d));
        }
        if (t % n != 0) {
        int n = m.length;
        }
            t += w;
        for (int w : m) {
        int t = 0; 
    public int findMinMoves(int[] m) {
        
        return r;
