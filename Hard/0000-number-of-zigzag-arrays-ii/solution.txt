class Solution {
    private static final int MOD = 1_000_000_007;

    public int zigZagArrays(int n, int l, int r) {
        int m = r - l + 1;
        int size = 2 * m;

        // Base case for n = 1
        if (n == 1) return m;

        // Construct the transition matrix T
        long[][] T = new long[size][size];
        
        // Block N: dp[i][y][0] = sum(dp[i-1][x][1]) for x > y
