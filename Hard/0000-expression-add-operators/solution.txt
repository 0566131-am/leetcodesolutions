                    backtrack(
                        i + 1, 
                        current_val - prev_term + (prev_term * part_val), 
                        prev_term * part_val, 
                        path_segments
                    )
                    path_segments.pop()

        backtrack(0, 0, 0, [])
        return results
