                peak = up;
                totalCandies += (1 + up);
            } else if (ratings[i] == ratings[i - 1]) {
                up = 0;
                down = 0;
                down = 0;
            if (ratings[i] > ratings[i - 1]) {
                up++;
        for (int i = 1; i < n; i++) {
        
        int peak = 0; 
        int down = 0;
        int up = 0;   
        int totalCandies = 1; 
        int n = ratings.length;
