        }
            }
                left[i] = left[i - 1] + 1;
            if (ratings[i] > ratings[i - 1]) {
        for (int i = 1; i < n; i++) {

        Arrays.fill(right, 1);
        Arrays.fill(left, 1);

        int[] right = new int[n];
        for (int i = n - 2; i >= 0; i--) {
            if (ratings[i] > ratings[i + 1]) {
                right[i] = right[i + 1] + 1;
            }
        }
