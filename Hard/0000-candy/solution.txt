            if (ratings[i] > ratings[i - 1]) {
                // Moving Up
        
        for (int i = 1; i < n; i++) {
        int peak = 0; // Keeps track of the height of the last peak
        int down = 0; // Keeps track of the length of the falling slope
        int up = 0;   // Keeps track of the length of the rising slope
        int totalCandies = 1; // First child always gets 1 candy
        
        int n = ratings.length;
        
        if (ratings == null || ratings.length == 0) return 0;
    public int candy(int[] ratings) {
class Solution {
