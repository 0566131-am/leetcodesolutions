                if (prefixSum[mid] > q) {
                    targetGcd = mid;
                    high = mid - 1; // Try to find a smaller 
                    valid GCD
                } else {
                    low = mid + 1;
                }
            }
            ans[i] = targetGcd;
        }
        
        return ans;
    }
}
