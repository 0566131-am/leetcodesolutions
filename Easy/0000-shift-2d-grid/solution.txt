                // index in 1D array (before rotation)
                int oldIndex = i * n + j;


            for (int j = 0; j < n; j++) {
        }

        for (int i = 0; i < m; i++) {
                row.add(0);
            ans.add(row);
            List<Integer> row = new ArrayList<>();
            for (int j = 0; j < n; j++)
        List<List<Integer>> ans = new ArrayList<>();

        for (int i = 0; i < m; i++) {

        k %= total;
        int total = m * n;

