            if a == 0:
                return b
            else:
                return a
        
        
        if str1 + str2 != str2 + str1:
            return ""
        
    
        gcd_length = gcd(len(str1), len(str2))
        
       
        return str1[0:gcd_length]
