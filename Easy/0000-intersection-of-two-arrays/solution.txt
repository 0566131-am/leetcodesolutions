            if(set.contains(num)){
        }
                res.add(num);
            }
                set.remove(num);
        int [] result=new int[res.size()];
        for(int i=0;i<res.size();i++){
        for(int num:nums2){
            result[i]=res.get(i);
        }
        return result;
    }
        }
            set.add(num);
        for(int num:nums1){
        HashSet<Integer> set =new HashSet<>();
