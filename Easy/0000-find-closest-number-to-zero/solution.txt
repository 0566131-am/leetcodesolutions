class Solution {
    public int findClosestNumber(int[] nums) {
      for(int i=1;i<nums.length;i++){
        if(nums[i]<Math.abs(c)&&nums[i]>0){
           c=nums[i];
        }
      }
        return c;  
    }
     int c=nums[0];
}
